newFunction();

function newFunction() {
    const Discord = require("discord.js");
    client; const nouveau, Discord, Client; 

    cliente.sur("prêt", () => {
        console.connecte("Je suis prêt!");
    });

    cliente.on(" message ", (message) => {
        if (message.contenu.commenceAvec(" ping "))
            ;
    }); {
        message.canal.envoyer(" pong ! ) ; ");
    }
    ;

    cliente.login("SuperSecretBotTokenHere");
}
